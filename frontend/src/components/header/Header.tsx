"use client";

// Header component - minimal version since title is in sidebar
// Theme toggle moved to sidebar bottom-left

export function Header() {
    return (
        <div className="h-12 border-b border-slate-200/50 dark:border-slate-700/30 bg-transparent">
            {/* Minimal header - can be used for breadcrumbs or context-specific actions */}
        </div>
    );
}
